# CODEX DELTA-PHI - GENIUSROUTER RCC-N SOFTWARE ARCHITECTURE INJECTION
## GeniusRouter-SA v0.4 - RCC-N Repository Navigation and Mini README Layer

Status: active RCC-N injection checkpoint
Date: May 2026
Repository: GeniusRouter
Current prior runtime layer: GeniusRouter-SA v0.3.2
Injection type: docs/context, rcc/nexus, mini README, route-map, checker, and validation-surface injection
Runtime mutation: none

---

## 1. Purpose

GeniusRouter-SA v0.4 injects RCC-N repository navigation into GeniusRouter after the runtime reached a smoke-validated state.

The goal is to make the repository easier for humans and AI coding agents to navigate without pretending that documentation proves code correctness.

The injection adds:

- Human / RCC Nexus / AI orientation surfaces,
- repository sphere coordinates,
- Echo Location blocks,
- route maps,
- folder-level mini READMEs,
- a checker script,
- machine-readable context indexes,
- validation reports,
- non-claim locks.

---

## 2. Source Lineage

This injection follows the OMN/RCC-N pattern:

- Human Director Box
- RCC Nexus README
- AI Agent README
- repository context index
- RCC Nexus index
- route map
- Echo Location records
- mini READMEs
- validation-bound AI operating protocol
- non-claim locks

This also follows RCC-N v1.0 principles:

- repository sphere,
- shell / meridian / sector coordinates,
- Echo Location records,
- Nexus Context Integrity,
- navigation entropy,
- geometric drift,
- validation-bound agent protocols.

---

## 3. What This Is

GeniusRouter-SA v0.4 is:

- an RCC-N injection layer,
- an AI-orientable repository navigation layer,
- a mini README coverage layer,
- a route-map and Echo Location layer,
- a validation surface for repository navigation,
- a preparation layer for later README trisection.

---

## 4. What This Is Not

GeniusRouter-SA v0.4 is not:

- not proof of routing quality,
- not proof of cost savings,
- not proof of production readiness,
- not proof of provider reliability,
- not proof of classifier accuracy,
- not proof of security,
- not proof that AI understands the repo,
- not proof that RCC-N validates code correctness,
- not a replacement for tests,
- not a replacement for benchmark evidence.

---

## 5. RCC-N Core Law for GeniusRouter

```text
RCC tells the agent what the repository means.
RCC-N tells the agent where it is.
Validation tells the agent whether reality agreed.
```

For GeniusRouter this becomes:

```text
Architecture explains the router.
RCC-N locates the router surfaces.
Tests and smoke validation decide what has actually passed.
```

---

## 6. Repository Sphere

| Shell | Name | GeniusRouter contents |
|---|---|---|
| center | Invariant Core | attribution, non-claim locks, source boundary, API-key secrecy, routing-evidence laws |
| inner | Runtime Primitives | `src/geniusrouter/`, config validation, routing, cache, schema, telemetry, provider abstraction |
| middle | Processes | tests, scripts, smoke runner, release scripts, RCC checker |
| outer | Evidence / Reflection | reports, artifacts, routing decisions, docs, release notes, validation reports |

---

## 7. Nexus Meridians

| Meridian | GeniusRouter meaning |
|---|---|
| source | original Keith / GeniusRouter attribution and project identity |
| runtime | FastAPI app, routing package, provider calls |
| validation | tests, smoke validation, py_compile, checker scripts |
| evidence | routing decision JSONL, validation reports, smoke reports |
| safety | API-key secrecy, non-claim locks, production-readiness boundaries |
| agent | AI operating contract, route map, mini READMEs |
| release | versioned architecture changes and release notes |
| documentation | docs registry, context index, RCC-N surfaces |

---

## 8. Echo Location Requirement

Every major folder README should include:

```text
RCC Nexus Echo Location
Sphere Position
Local Role
Inbound Hooks
Outbound Hooks
Evidence Surface
Validation Surface
Claim Boundary
Non-Claim Locks
Agent Route
Update Obligation
```

This lets the next human or AI agent locate the folder before editing it.

---

## 9. AI Agent Route

Before modifying GeniusRouter, an AI agent must read:

1. `README.md`
2. `README_5_MINUTES.md`
3. `docs/context/repository_context_index.json`
4. `docs/context/rcc_nexus_index.json`
5. `rcc/nexus/route_map.json`
6. `docs/protocols/ai_operating_contract.md`
7. the target folder `README.md`
8. relevant source/tests/docs
9. validation commands

---

## 10. Validation Commands

Current required validation after RCC-N changes:

```powershell
python scripts/rcc/check_rcc_nexus.py
python -m unittest discover -s tests
python scripts/release/run_smoke_validation_v0_3.py
```

---

## 11. Done Criteria

A GeniusRouter RCC-N change is not complete until:

- original attribution is preserved,
- mini READMEs exist for indexed folders,
- route map exists,
- RCC Nexus index exists,
- checker passes,
- unit tests pass,
- smoke validation passes,
- claim boundaries are preserved,
- docs registry is updated.

---

## 12. v0.4 Law

```text
No AI-agent navigation claim without mini READMEs, route maps, context index, checker, and validation.
```

---

## 13. Boundary

RCC-N improves navigation, traceability, and AI-agent self-location. It does not prove routing correctness, benchmark validity, production readiness, security, patch safety, or AI understanding.